Inspect each typed specialist error and continue deliberately. Correct the first invalid call, retry the token-exhausted child with a smaller task, then verify that the child-specific retry bound also returns control to the parent.

FAKE_TOOL_SEQUENCE: [{"name":"researcher","args":{}},{"name":"researcher","args":{"task":"This deliberately oversized task exhausts the specialist-local token budget before the result can be accepted. It contains enough detail to make the first delegated prompt materially larger than the retry: investigate every possible source, enumerate all alternatives, compare every tradeoff, preserve every observation, explain every uncertainty, and produce a comprehensive report even though the parent only needs a concise answer. Repeat the unnecessary scope once more: investigate every possible source, enumerate all alternatives, compare every tradeoff, preserve every observation, explain every uncertainty, and produce a comprehensive report."}},{"name":"researcher","args":{"task":"Return only the concise result."}},{"name":"researcher","args":{"task":"This call must be rejected by max_calls without failing the parent."}}]
FAKE_AGENT_FINAL:
{"status":"retried_child_and_respected_retry_bound"}
